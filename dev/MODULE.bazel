bazel_dep(name = "rules_cc", version = "0.2.17")

local_repository = use_repo_rule("@bazel_tools//tools/build_defs/repo:local.bzl", "new_local_repository")

local_repository(
    name = "local_tool",
    path = "/usr/local/bin",
    build_file_content = """
exports_files(["bsc", "yosys"])
""",
)